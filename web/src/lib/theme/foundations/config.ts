import { ThemeConfig } from '@chakra-ui/react';

const config: ThemeConfig = {
  cssVarPrefix: 'puzzlely',
  useSystemColorMode: true,
  initialColorMode: 'system',
  disableTransitionOnChange: true,
};

export default config;
