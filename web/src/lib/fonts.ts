import { Inter, Raleway } from "next/font/google";

export const BODY_FONT = Inter({
  subsets: ["latin"],
  variable: "--font-body",
});

export const HEADING_FONT = Raleway({
  subsets: ["latin"],
  variable: "--font-heading",
});
